## with triangles we already have implicit segments
## so we want to find those, keeping the triangle grouping
## and bisect each one as a simple subdivision for greater detail


bisegment <- function(idx, xy) {
  mat <- matrix(idx, nrow = 3)
  x <- xy[mat[,1], ]
  ## rah rah
}

